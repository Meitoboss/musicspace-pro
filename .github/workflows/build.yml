name: Build iOS IPA

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: macos-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Xcode
      uses: maxim-lobanov/setup-xcode@v1
      with:
        xcode-version: 'latest-stable'

    - name: Build Archive
      run: |
        xcodebuild archive \
          -scheme "YourAppName" \
          -archivePath build/YourAppName.xcarchive \
          -sdk iphoneos \
          CODE_SIGNING_ALLOWED=NO

    - name: Export IPA
      run: |
        mkdir -p build/IPA
        cd build/YourAppName.xcarchive/Products/Applications/
        zip -r ../../../IPA/YourAppName.ipa *.app

    - name: Upload IPA Artifact
      uses: actions/upload-artifact@v4
      with:
        name: iOS-App-IPA
        path: build/IPA/YourAppName.ipa